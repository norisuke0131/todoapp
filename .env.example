# TypeSafe API の認証キー（本番モードで必須）。https://typesafe.ai で取得
TYPESAFE_API_KEY=sk-...

# 使用モデルの上書き（任意）。未設定なら SDK 既定の jev-latest
# TYPESAFE_DEFAULT_MODEL=jev-latest

# ログレベルの上書き（任意）: debug / info / warn / error / off
# TYPESAFE_LOG_LEVEL=warn
