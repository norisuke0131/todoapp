{
  "name": "todoapp",
  "version": "0.1.0",
  "private": true,
  "description": "TypeSafe (System One) でタスクを自動分類・優先度づけする最小CLIデモ",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "todo-classify": "dist/cli.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "npm run typecheck && esbuild src/cli.ts --bundle --platform=node --format=esm --packages=external --banner:js=\"#!/usr/bin/env node\" --outfile=dist/cli.js",
    "classify": "node dist/cli.js",
    "demo": "node dist/cli.js --demo"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^3.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
